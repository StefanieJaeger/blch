import VotingList from "./VotingList";

const VoterPanel = () => {
    return (
        <div>
            <h2>Voter Panel</h2>
            <p>You are logged in as a voter.</p>
            <VotingList />
            
        </div>
    );
};

export default VoterPanel;
